<div class="mx-auto max-w-md">
  <h1 class="text-2xl font-bold tracking-tight">Forgot your password?</h1>

  <div class="mt-6 rounded-lg border border-gray-200 bg-white p-6">
    <%= form_with url: passwords_path, class: "space-y-4" do |form| %>
      <div>
        <%= form.label :email_address, "Email address", class: "block text-sm font-medium text-gray-700" %>
        <%= form.email_field :email_address, required: true, autofocus: true, autocomplete: "username", value: params[:email_address],
              class: "mt-1 w-full rounded-md border border-gray-300 px-3 py-2 text-sm focus:border-gray-500 focus:outline-none" %>
      </div>

      <%= form.submit "Email reset instructions", class: "w-full rounded-md bg-gray-900 px-4 py-2 text-sm font-medium text-white hover:bg-gray-700 cursor-pointer" %>
    <% end %>

    <%= link_to "Back to sign in", new_session_path, class: "mt-4 inline-block text-sm text-gray-600 hover:text-gray-900" %>
  </div>
</div>
